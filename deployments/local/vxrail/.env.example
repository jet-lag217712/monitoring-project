# Local VxRail (Debian VM) — points at Mac host Mosquitto.

# Mac host IP as seen from the Debian VM (VMware Fusion shared/host network).
MQTT_BROKER=tls://REPLACE_WITH_MAC_HOST_IP:8883
MQTT_PASSWORD=secret

COLLECTOR_ADMIN_PORT=9090
