# Dev VxRail (GNS3 / on-prem) — points at Azure Mosquitto.

# Azure cloud Mosquitto (replace with your VM public/private IP or DNS).
MQTT_BROKER=tls://REPLACE_WITH_AZURE_HOST:8883
MQTT_PASSWORD=secret

COLLECTOR_ADMIN_PORT=9090
