# Retired split runtime

This directory is retained only for repository compatibility and test fixtures.
It is not a supported or documented Equate installation path.

Use the complete local appliance in [`../appliance/`](../appliance/). That
runtime places the collectors, MQTT, ingestion, PostgreSQL, API, and dashboard
on one VxRail- or VMware-type VM and configures sites through the setup TUI.
