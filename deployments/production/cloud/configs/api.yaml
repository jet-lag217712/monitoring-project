api:
  listen: ":8000"
  cors_origins: "https://CHANGE_ME.example.com"

admin:
  listen: ":9092"

auth:
  enabled: true
  google_client_id_env: "GOOGLE_CLIENT_ID"
  google_client_id: ""

database:
  url_env: "DATABASE_URL"
  max_conns: 10
  min_conns: 1
  max_lifetime: 1h

online_threshold: 5m
