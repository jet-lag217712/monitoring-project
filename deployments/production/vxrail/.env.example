# Production on-site collector environment (copy to .env — never commit secrets).

MQTT_BROKER=tls://CHANGE_ME.example.com:8883
MQTT_PASSWORD=CHANGE_ME
COLLECTOR_ADMIN_PORT=9090

# SNMP community (set locally; never commit the real value).
SNMP_COMMUNITY_CORE_SWITCH=CHANGE_ME
