# Retired infrastructure directory

This directory is retained only for historical repository compatibility. It is
not part of the supported Equate product or release workflow.

The supported product is the local appliance, which uses the bundled
PostgreSQL and Mosquitto containers on the appliance VM. Use
[`docs/releases/appliance-ova.md`](../../docs/releases/appliance-ova.md) and
[`deployments/production/appliance/`](../../deployments/production/appliance/)
for current installation and release documentation.
